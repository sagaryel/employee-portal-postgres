{
  "name": "employee-portal-postgres",
  "version": "1.0.0",
  "description": "employee portal postgres sql database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sagar yelgond",
  "license": "ISC",
  "devDependencies": {
    "serverless-iam-roles-per-function": "^3.2.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.575.0",
    "@aws-sdk/util-dynamodb": "^3.575.0",
    "pg": "^8.11.5",
    "sequelize": "^6.37.3"
  }
}
